<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.hospitalserver.mapper.UserMapper">
    <!-- 这里写接口文件里对应的SQL映射-->
    <!-- 用户登录验证 -->
    <select id="validateUser" parameterType="com.hospital.hospitalserver.entity.UserInfo" resultType="com.hospital.hospitalserver.entity.UserInfo">
        select user_id AS userId,
               username
        from shequdb.users where username = #{username} and password = #{password}
    </select>
</mapper>